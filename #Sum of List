#Sum of List
def SumofList(N):
    Sum =0
    a = []
    N = int(input("Enter the Length of list you want"))
    for i in range(N):
        a.append(int(input("Enter the Value you want : " )))
        Sum += a[i]
    print(Sum)

#def PrintSum():
print(SumofList(5))
